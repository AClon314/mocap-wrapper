name: dynhamr
on:
  push:
    branches: ["main"]
    tags: ["v*.*.*"]
    paths:
      - "docker/dynhamr/**"
      - ".github/workflows/dynhamr.yml"
  pull_request:
    branches: ["main"]
    paths:
      - "docker/dynhamr/**"
      - ".github/workflows/dynhamr.yml"
  workflow_dispatch:
permissions:
  contents: read
  packages: write
jobs:
  BP:
    uses: ./.github/workflows/build_push.yml
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
      HF_TOKEN: ${{ secrets.HF_TOKEN }}
    with:
      name: dynhamr
      remainGB_py_env: 20
      remainGB_final: 49