name: Large Docker Image Build & Push

on:
  workflow_call:
    inputs:
      name:
        required: true
        type: string
        description: "The name of the Docker image (e.g., gvhmr,wilor...)"
      remain_gb:
        required: false
        type: number
        default: 21
        description: "The remaining disk space in GB before FINAL build & push. Set to <=21 GB to skip cleaning."
    secrets:
      DOCKERHUB_USERNAME:
        required: true
      DOCKERHUB_TOKEN:
        required: true
      HF_TOKEN:
        required: true

env:
  NAME: ${{ inputs.name }}

permissions:
  contents: read
  packages: write

jobs:
  model_weights:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/pre
        id: pre
        with:
          remain_gb: ${{ inputs.remain_gb }}
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 💾Cache model_weights
        uses: docker/build-push-action@v5
        with:
          context: ./docker
          file: docker/${{ env.NAME }}/${{ env.NAME }}.dockerfile
          target: model_weights
          push: true
          tags: ghcr.io/${{ steps.pre.outputs.owner }}/${{ env.NAME }}:model_weights
          cache-from: |
            type=registry,ref=ghcr.io/${{ steps.pre.outputs.owner }}/${{ env.NAME }}:model_weights
          cache-to: |
            type=registry,ref=ghcr.io/${{ steps.pre.outputs.owner }}/${{ env.NAME }}:model_weights,compression=zstd
          build-args: |
            HF_TOKEN=${{ secrets.HF_TOKEN }}

      - run: df -h ; find . -not -path "*/.*" -not -name ".*" | grep -vE 'pyc|swp|__init' | sed -e "s/[^-][^\/]*\// |/g" -e "s/|\([^ ]\)/|-\1/"
        if: always()

  # 并行构建 py_env cache
  py_env:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/pre
        id: pre
        with:
          remain_gb: ${{ inputs.remain_gb }}
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 💾Cache py_env
        uses: docker/build-push-action@v5
        with:
          context: ./docker
          file: docker/${{ env.NAME }}/${{ env.NAME }}.dockerfile
          target: py_env
          push: true
          tags: ghcr.io/${{ steps.pre.outputs.owner }}/${{ env.NAME }}:py_env
          cache-from: |
            type=registry,ref=ghcr.io/${{ steps.pre.outputs.owner }}/${{ env.NAME }}:py_env
          cache-to: |
            type=registry,ref=ghcr.io/${{ steps.pre.outputs.owner }}/${{ env.NAME }}:py_env,compression=zstd
          build-args: |
            HF_TOKEN=${{ secrets.HF_TOKEN }}

      - run: df -h ; find . -not -path "*/.*" -not -name ".*" | grep -vE 'pyc|swp|__init' | sed -e "s/[^-][^\/]*\// |/g" -e "s/|\([^ ]\)/|-\1/"
        if: always()

  # 等待上面两个完成，再用 cache 构建并推送最终鏡像
  push:
    needs: [model_weights, py_env]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/pre
        id: pre
        with:
          remain_gb: ${{ inputs.remain_gb }}
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Login to DockerHub
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          username: ${{ steps.pre.outputs.dockerhub }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Get current date
        id: date
        run: echo "date=$(date +'%Y.%m')" >> $GITHUB_OUTPUT

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/${{ steps.pre.outputs.owner }}/${{ env.NAME }}
            ${{ steps.pre.outputs.dockerhub }}/${{ env.NAME }}
          tags: |
            type=raw,value=${{ github.ref_name }},enable=${{ startsWith(github.ref, 'refs/tags/') }}
            type=raw,value=latest,enable=${{ github.ref_name == github.event.repository.default_branch }}
            type=raw,value=${{ github.ref_name }}-${{ github.sha }},enable=true
            type=raw,value=${{ steps.date.outputs.date }},enable=true
          github-token: ${{ secrets.GITHUB_TOKEN }}
          context: workflow

      - uses: ./.github/actions/if_clean
        with:
          remain_gb: ${{ inputs.remain_gb }}

      - name: 🔨Build final
        uses: docker/build-push-action@v5
        with:
          context: ./docker
          file: docker/${{ env.NAME }}/${{ env.NAME }}.dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: |
            type=registry,ref=ghcr.io/${{ steps.pre.outputs.owner }}/${{ env.NAME }}:model_weights
            type=registry,ref=ghcr.io/${{ steps.pre.outputs.owner }}/${{ env.NAME }}:py_env
          build-args: |
            HF_TOKEN=${{ secrets.HF_TOKEN }}

      - run: df -h ；find . -not -path "*/.*" -not -name ".*" | grep -vE 'pyc|swp|__init' | sed -e "s/[^-][^\/]*\// |/g" -e "s/|\([^ ]\)/|-\1/"
        if: always()
